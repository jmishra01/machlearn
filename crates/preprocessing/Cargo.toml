[package]
name = "machlearn-preprocessing"
description = "Feature scaling, imputation, encoding, and preprocessing pipelines for MachLearn"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[features]
csv = ["dep:csv"]
serde = ["dep:serde", "ndarray/serde", "machlearn-core/serde"]
arrow = ["dep:arrow-array", "dep:arrow-schema"]

[dependencies]
ndarray.workspace = true
csv = { workspace = true, optional = true }
serde = { workspace = true, optional = true }
machlearn-core = { path = "../core", version = "0.1.0" }
arrow-array = { workspace = true, optional = true }
arrow-schema = { workspace = true, optional = true }
